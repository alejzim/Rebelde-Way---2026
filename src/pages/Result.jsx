import { ArrowRight, RotateCcw, Star } from 'lucide-react';
import { CHARACTERS } from '../../shared/quiz.js';
import CharacterImage from '../components/CharacterImage.jsx';
export default function Result({ characterId, name, onRestart }) {
  const character = CHARACTERS[characterId];
  return <section className={`result-page result-${characterId} page-enter`} style={{ '--character-color': character.color }}><div className="result-photo-wrap"><div className="result-tape" /><div className="result-photo"><CharacterImage key={characterId} character={character} /><span className="result-photo-label">{character.name}</span></div><span className="result-photo-note">La rebeldía también se lleva adentro. ♡</span><Star className="result-star" size={65} strokeWidth={1.3} /></div><div className="result-copy"><div className="eyebrow"><span className="status-dot" /> YA ENCONTRASTE TU VOZ</div><p className="result-greeting">{name ? `${name}, tu lado rebelde tiene nombre.` : 'Tu lado rebelde tiene nombre.'}</p><h1>¡Sos <span>{character.shortName}!</span></h1><p className="result-full-name">{character.name}</p><div className="result-tags">{character.tags.map(tag => <span key={tag}>{tag}</span>)}</div><p className="result-description">{character.description}</p><blockquote>“{character.quote}”</blockquote><button className="button button-red" onClick={onRestart}><RotateCcw size={17} /> Volver a hacer el test <ArrowRight size={18} /></button><p className="result-footnote">Un test para divertirte y volver a esa época.<br />Siempre podés tener un poco de los cuatro.</p></div></section>;
}
